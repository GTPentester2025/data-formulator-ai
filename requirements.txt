# Core dependencies
pandas
flask
flask-cors
flask-limiter
openai
python-dotenv
# litellm 1.92+ is Rust/maturin (manylinux-only wheels) — hangs on Windows/macOS
# without a Rust toolchain. Pin below it; >=1.84.0 keeps the CVE fix.
# NOTE: every release in this range declares Requires-Python <3.14, so the app
# must run on 3.11–3.13.
litellm>=1.84.0,<1.92
duckdb
numpy
vl-convert-python
beautifulsoup4
scikit-learn
pyyaml
pyarrow>=13.0.0
xlrd
openpyxl>=3.1.0
lxml

# File upload only. Every external data-loader dependency was removed with the
# loaders themselves — no database drivers (mysql, postgres, mssql, clickhouse,
# mongo, cosmos), no cloud SDKs (boto3, azure-*, google-cloud-bigquery,
# databricks), no BI clients (superset) and no market-data client (yfinance).
# Nothing here opens an outbound connection on the app's behalf except the LLM
# endpoint the user configures.

# SSO / Auth deps
PyJWT[crypto]>=2.8.0
requests
flask-session>=0.8.0
-e .
