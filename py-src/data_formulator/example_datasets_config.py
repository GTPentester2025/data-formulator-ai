"""Sample datasets offered in the UI.

Deliberately EMPTY for this deployment. The upstream list pulled CSV and JSON
straight from raw.githubusercontent.com the moment a user opened the dataset
panel, which is both an outbound call this server must not make and a set of
demo tables nobody here wants. The module stays so the imports around it keep
working; the UI simply shows no samples.
"""

EXAMPLE_DATASETS = []
